---
description: Create a private GitHub repo via gh CLI and push the current project
---

Run this Bash command from the user's current working directory and show its output verbatim:

```bash
~/.claude-project-kit/kit/scripts/proj-init-remote
```

If the command fails because `git` or `gh` are not installed, explain what the user needs to install.
